{
  "name": "artync-site",
  "version": "1.0.0",
  "private": true,
  "description": "Landing institucional Artync — HTML estático + React via CDN. JSX pré-compilado por esbuild.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "serve": "python -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
